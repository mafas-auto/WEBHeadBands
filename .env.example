# Environment Variables Template
# Copy this file to .env.local and fill in your values

# Cloudflare Pages Environment Variables
# These can be set in Cloudflare Dashboard â†’ Pages â†’ Your Project â†’ Settings â†’ Environment Variables

# Example: API Keys (if needed in the future)
# VITE_API_KEY=your_api_key_here

# Example: Feature Flags
# VITE_ENABLE_ANALYTICS=false

# Note: All Vite env variables must be prefixed with VITE_ to be exposed to the client
# Access them in code using: import.meta.env.VITE_API_KEY
